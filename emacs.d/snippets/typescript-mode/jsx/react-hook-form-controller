# -*- mode: snippet -*-
# name: react hooks form with controller
# key: form-ctrl
# --
<Controller
  name='$1'
  control={control}
  render={({ field: { value, onChange }, fieldState: { error } }) => (
    $2
  )}
/>
